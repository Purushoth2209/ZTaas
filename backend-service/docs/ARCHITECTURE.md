# Architecture Flow Diagram

## 🔄 JWT Issuance Flow (RS256)

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ 1. POST /auth/login
       │    { username, password }
       ▼
┌─────────────────────────────────────────┐
│         Backend Service                 │
│         (localhost:5001)                │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │  auth.service.js                 │  │
│  │  ┌────────────────────────────┐  │  │
│  │  │ 1. Validate credentials    │  │  │
│  │  │ 2. Load private.key        │  │  │
│  │  │ 3. Sign JWT with RS256     │  │  │
│  │  │    - sub: user.id          │  │  │
│  │  │    - username              │  │  │
│  │  │    - role                  │  │  │
│  │  │    - iss: localhost:5001   │  │  │
│  │  │    - aud: api-gateway      │  │  │
│  │  │    - kid: backend-key-1    │  │  │
│  │  └────────────────────────────┘  │  │
│  └──────────────────────────────────┘  │
└─────────────┬───────────────────────────┘
              │
              │ 2. Response
              │    { accessToken: "eyJ..." }
              ▼
       ┌─────────────┐
       │   Client    │
       │ (has JWT)   │
       └─────────────┘
```

## 🔓 JWKS Public Key Distribution

```
┌─────────────────┐
│  API Gateway    │
│  or External    │
│  Service        │
└────────┬────────┘
         │
         │ GET /.well-known/jwks.json
         ▼
┌─────────────────────────────────────────┐
│         Backend Service                 │
│         (localhost:5001)                │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │  jwks.routes.js                  │  │
│  │  ┌────────────────────────────┐  │  │
│  │  │ jwk.util.js                │  │  │
│  │  │ ┌────────────────────────┐ │  │  │
│  │  │ │ 1. Load public.key     │ │  │  │
│  │  │ │ 2. Convert to JWK      │ │  │  │
│  │  │ │ 3. Return JWKS JSON    │ │  │  │
│  │  │ └────────────────────────┘ │  │  │
│  │  └────────────────────────────┘  │  │
│  └──────────────────────────────────┘  │
└─────────────┬───────────────────────────┘
              │
              │ Response:
              │ {
              │   "keys": [{
              │     "kty": "RSA",
              │     "use": "sig",
              │     "alg": "RS256",
              │     "kid": "backend-key-1",
              │     "n": "...",
              │     "e": "AQAB"
              │   }]
              │ }
              ▼
       ┌─────────────────┐
       │  API Gateway    │
       │ (has public key)│
       └─────────────────┘
```

## 🛡️ JWT Verification Flow (Gateway Side)

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ Request with JWT
       │ Authorization: Bearer eyJ...
       ▼
┌─────────────────────────────────────────┐
│         API Gateway                     │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │  JWT Verification                │  │
│  │  ┌────────────────────────────┐  │  │
│  │  │ 1. Extract kid from header │  │  │
│  │  │ 2. Fetch matching key from │  │  │
│  │  │    cached JWKS             │  │  │
│  │  │ 3. Verify signature with   │  │  │
│  │  │    public key (n, e)       │  │  │
│  │  │ 4. Validate claims:        │  │  │
│  │  │    - iss = localhost:5001  │  │  │
│  │  │    - aud = api-gateway     │  │  │
│  │  │    - exp not expired       │  │  │
│  │  └────────────────────────────┘  │  │
│  └──────────────────────────────────┘  │
└─────────────┬───────────────────────────┘
              │
              │ ✅ Valid JWT
              │ Forward to backend
              ▼
       ┌─────────────────┐
       │ Backend Service │
       │ /users, /orders │
       └─────────────────┘
```

## 🔑 Key Management Architecture

```
┌─────────────────────────────────────────┐
│         Backend Service                 │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │  src/keys/                       │  │
│  │  ┌────────────┐  ┌────────────┐  │  │
│  │  │ private.key│  │ public.key │  │  │
│  │  │            │  │            │  │  │
│  │  │ 🔒 SECRET  │  │ 🌐 PUBLIC  │  │  │
│  │  │            │  │            │  │  │
│  │  │ Used for:  │  │ Used for:  │  │  │
│  │  │ - Signing  │  │ - JWKS     │  │  │
│  │  │   JWTs     │  │ - Internal │  │  │
│  │  │            │  │   verify   │  │  │
│  │  └────────────┘  └────────────┘  │  │
│  └──────────────────────────────────┘  │
│                                         │
│  ┌──────────────────────────────────┐  │
│  │  jwt.config.js                   │  │
│  │  - Loads both keys               │  │
│  │  - Defines algorithm (RS256)     │  │
│  │  - Sets kid, iss, aud            │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

## 📊 Before vs After

### Before (HS256 - Shared Secret)
```
Backend                    Gateway
   │                          │
   │  Share JWT_SECRET        │
   │ ◄────────────────────────┤
   │                          │
   │  Sign with secret        │
   │  Verify with secret      │
   │                          │
   └──────────────────────────┘
        🔒 Both need secret
```

### After (RS256 - Public/Private Keys)
```
Backend                    Gateway
   │                          │
   │  Expose JWKS (public)    │
   │ ─────────────────────────►
   │                          │
   │  Sign with private key   │
   │  Verify with public key  │
   │                          │
   └──────────────────────────┘
        🔓 No shared secrets!
```

## 🎯 Security Benefits

1. **No Shared Secrets**: Gateway never needs private key
2. **Standard Protocol**: JWKS is industry standard (OAuth 2.0, OIDC)
3. **Key Rotation Ready**: kid enables multiple keys in future
4. **Asymmetric Crypto**: Private key compromise doesn't affect verification
5. **Scalable**: Multiple gateways can fetch same JWKS

## 🚀 Integration Points

```
┌──────────────┐
│   Client     │
└──────┬───────┘
       │
       │ 1. Login
       ▼
┌──────────────┐     3. Fetch JWKS      ┌──────────────┐
│   Backend    │◄──────────────────────│ API Gateway  │
│  (Issuer)    │                        │ (Verifier)   │
└──────┬───────┘                        └──────┬───────┘
       │                                       │
       │ 2. JWT                                │
       └───────────────────────────────────────┤
                                               │
                                               │ 4. Verify & Forward
                                               ▼
                                        ┌──────────────┐
                                        │   Backend    │
                                        │  Resources   │
                                        └──────────────┘
```
